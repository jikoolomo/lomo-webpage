# LOMO Links 개선 내역

## 배포 안정성

오디오 소스를 `/manus-storage/` 절대 경로에서 `audio/` 상대 경로로 변경했습니다. 이제 `audio/` 폴더를 웹페이지와 함께 GitHub Pages 배포 루트에 업로드하면 자연음 파일을 찾을 수 있습니다.

인트로·히어로·생태계 카드·LOMO House 모달에서 사용하는 사진을 `images/` 폴더에 포함했습니다. 페이지 핵심 이미지가 외부 이미지 서버 상태에만 의존하지 않도록 했으며, 사용자의 사진으로 교체할 때도 파일명을 유지하거나 CSS의 경로만 바꾸면 됩니다.

## 사용자 경험과 접근성

사운드 버튼의 선택 상태를 브라우저 저장소에 저장하고, 이전에 사운드를 허용한 방문자는 첫 사용자 상호작용 이후 선호 상태를 복원하도록 했습니다. 오디오 로딩 오류가 발생하면 사운드를 안전하게 끄고 버튼 상태를 갱신합니다.

공유 미리보기용 Open Graph와 Twitter 메타데이터, 작성자 정보, 이미지 연결을 추가했습니다. 히어로 이미지는 우선 로딩하도록 설정했고, 새 탭으로 열리는 외부 링크에는 `noopener`를 추가했습니다.

인트로가 열려 있을 때 본문 스크롤이 발생하지 않도록 잠금 처리했으며, 브랜드·Route Studio·푸터 링크에도 키보드 포커스 표시를 추가했습니다. 기존의 `prefers-reduced-motion`, 모달 Escape 닫기, 포커스 복원 동작은 유지했습니다.

## 첨부 검토 반영

첨부 스크린샷에서 미완성으로 보인 나침반을 단일 하단 선 대신 양방향 바늘·중심점·북쪽 표기가 있는 SVG 심볼로 교체했습니다. 지도·필드노트·이동이라는 기존 메타포는 유지하되, 사진을 압도하지 않도록 얇은 청록색 스타일을 유지했습니다.

“물이 무서웠습니다. 지금은 다이버예요.” 문장은 삭제하지 않았습니다. 이 문장이 LOMO의 추상적인 지구시민 세계관을 Jikoo On의 실제 변화와 바다 테마에 연결하는 origin note 역할을 하기 때문입니다. 대신 별도 장식으로 과도하게 강조하지 않고 기존 히어로 계층 안에 남겼습니다.

Claude Code의 실행 검토에서 재현된 모달 닫기 타이머 레이스, 인트로 인터벌 누수, 단일 오디오 트랙 오류의 전체 사운드 차단, 오디오 페이드 중첩을 수정했습니다. 모달은 재개방 시 숨김 타이머를 취소하고 배경 콘텐츠를 `inert` 처리합니다. 인트로 건너뛰기는 인터벌을 즉시 정리합니다.

이미지를 WebP로 최적화하고 사막 인트로에는 가로형 크롭을 적용했습니다. 더 이상 사용하지 않는 Unsplash 이미지 preconnect도 제거했습니다.

## 다국어 지원

헤더에 `한국어 / EN` 언어 선택기를 추가하고, 인트로·히어로·생태계·Route Studio·Waypoints·LOMO House 모달·접근성 라벨을 한국어와 영어로 전환할 수 있게 했습니다. 사용자의 언어 선택은 `localStorage`에 저장됩니다. 저장된 선택이 없으면 브라우저 언어가 `ko` 또는 `ko-*`일 때 한국어로 시작하고, 그 외에는 영어로 시작합니다. 영어 초기 렌더링 중 한국어가 잠깐 보이는 현상도 방지했습니다.

## Intro 가독성 개선

인트로 장면 전환 시간을 약 680ms에서 1050ms로 늘렸습니다. 네 가지 환경의 문장이 시각적 플래시로만 지나가지 않고 짧은 서문으로 읽힐 수 있도록 조정했으며, 건너뛰기 기능은 그대로 유지했습니다. 전체 인트로는 약 5초 이내에 끝납니다.

## Earth Citizen 범위 확장

GPT 검토에서 지적한 ‘자연·여행·사진’ 중심의 좁은 인상을 보완하기 위해 기록의 생태계 설명을 확장했습니다. 기존 SEA / FOREST / DESERT / MOSS는 LOMO의 전체 정의가 아니라 현재 시각 언어로 유지하고, 그 아래에 `MOVE / LIVE / MAKE / RECORD / DWELL` 실천 영역을 추가했습니다. LOMO가 앞으로 도시·기술·도구·관계·집·실험으로 확장될 수 있는 구조임을 짧게 드러냅니다.

검토에서 가장 큰 개선 여지로 평가된 이미지 진정성은 코드 변경만으로 해결하지 않고, 실제 Jikoo On의 사진을 받을 수 있도록 `IMAGE-BRIEF.md`에 섹션별 요구사항을 정리했습니다. 현재 스톡 이미지를 임의로 교체하지 않고, 히어로·Route Studio·Waypoint·LOMO House·OG 이미지의 용도와 권장 구도를 구분해 두었습니다.

## 사운드 정책 변경

새 LOMO 페이지를 열면 `soundEnabled`를 ON으로 초기화하도록 변경했습니다. 브라우저 자동재생 정책을 존중하기 위해 실제 `play()` 호출은 첫 사용자 상호작용 후 실행될 수 있습니다.

`visibilitychange`에서 페이지가 hidden이 되면 모든 트랙을 즉시 pause하고 재생 위치를 초기화합니다. `pagehide`에서도 동일하게 정리해 다른 웹페이지로 이동하거나 문서가 unload되는 경우 음악이 남지 않도록 했습니다. LOMO 페이지로 돌아온 뒤에는 다시 사용자 상호작용이 있을 때 현재 테마의 음원이 재생됩니다.

## SEO 추가 설정

`robots.txt`를 추가하고 `index.html`에 `robots` 메타 태그, Open Graph·Twitter 카드, `WebSite`/`Person` JSON-LD를 추가했습니다. 실제 GitHub Pages 도메인이 아직 확정되지 않았으므로 canonical URL과 `sitemap.xml`은 의도적으로 넣지 않았으며, 배포 주소 확정 후 추가해야 합니다.

## 렌더링 미리보기

임시 공개 미리보기에서 인트로, 메인 히어로, LOMO House 모달을 브라우저로 렌더링해 확인했습니다. 캡처 기록은 `render-notes.md`에 있습니다.

## 검증 결과

`script.js`는 Node 문법 검사를 통과했습니다. 네 개 오디오 파일은 로컬 공개 미리보기 서버에서 HTTP 200으로 응답했고, 인트로 재생 URL(`?intro=1`), 인트로 건너뛰기, 메인 화면 전환, LOMO House 모달 열기·닫기, 사운드 버튼 상태 변경을 브라우저에서 확인했습니다.

## 배포 구성

GitHub Pages에는 다음 항목을 배포 루트에 함께 업로드합니다.

| 항목 | 설명 |
|---|---|
| `index.html` | 페이지 구조와 콘텐츠 |
| `styles.css` | 시각 디자인과 반응형 스타일 |
| `script.js` | 인트로·사운드·모달 상호작용 |
| `audio/` | 네 가지 자연음 MP3 |
| `images/` | 페이지에서 사용하는 로컬 사진 |
| `robots.txt` | 기본 검색엔진 크롤링 허용 정책 |
